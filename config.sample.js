exports.config = config = {
	
	// Twilio Acount SID
	accountSID: '',

	// Twilio Auth Token
	authToken: '',

	// SMS number from Twilio account
	fromNumber: '',
	
	// Accela config options.
	accela: { 
		config : { 
				app_id: '',
				app_secret: '',
				access_token: '',
				environment: 'PROD',
				agency: 'ATLANTA_GA',
				module: 'Building'
			}
		}
		
}
